name: CI

on: push

jobs:
  build-and-unit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Files
        uses: actions/checkout@v3

      - name: Build and Test
        uses: ./.github/actions/build
